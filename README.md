# 言序

言序是一个本地优先的结构化表达训练应用，面向面试、工作汇报和项目复盘场景。

## 当前版本

当前里程碑是 Mac 优先的可运行开发版，包含响应式界面、本地会话持久化、知识库基础匹配和可替换的转写能力接口。真实本地语音模型、Android 工程与跨端同步将在后续里程碑接入。

## 本地运行

```bash
pnpm install
pnpm dev
```

启动 Tauri 桌面应用：

```bash
pnpm tauri dev
```

## 验证

```bash
pnpm test
pnpm build
```
